// @/components/IOSPaymentMethods.tsx
// Stub — react-native-iap removed (SDK 57 compatibility)
import React from 'react';

const IOSPaymentMethods = ({ onSuccess, onFailure }: { onSuccess?: () => void; onFailure?: () => void }) => {
  return null;
};

export default IOSPaymentMethods;