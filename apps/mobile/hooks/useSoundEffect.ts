// @/hooks/useSoundEffect.ts
// Stub — expo-av removed (SDK 57 compatibility)

/**
 * Custom hook to handle sound effects in the app.
 * Currently stubbed — expo-av was removed for SDK 57 compatibility.
 */
export const useSoundEffect = () => {
  // no-op
};