import type { SampleContent } from './types';

/** Neutral placeholder until a language's sample is authored. */
const content: SampleContent = {
  title: 'Sample',
  short: 'A sample text for this language is coming soon.',
  long: null,
};

export default content;
